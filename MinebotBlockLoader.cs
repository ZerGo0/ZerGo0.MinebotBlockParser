using System;

public class Blocks
{
    public Blocks(ushort id, ushort metadata)
    {
        ID = id;
        Metadata = metadata;
    }

    public ushort ID;
    public ushort Metadata { get; set; }

    public string toString()
    {
        return this.
    }
}
